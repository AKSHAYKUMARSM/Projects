let name="AKS";
let role="Admin";
const title=document.querySelector("title");
title.innerText=`${name} | ${role}`;
const welcome=document.getElementById("title");
welcome.innerText=`Welcome ${role}: ${name}`;